<section class="hero">
    <div class="text-container">
        <h1>{{title}}</h1>
        <h4>{{summary}}</h4>
        <ul class="flex-container tag-container">
            {{#each tag}}
            <li class="flex-item">{{this}}</li>
            {{/each}}
        </ul> 
    </div>
</section>