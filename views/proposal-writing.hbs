<section class="hero">
    <div class="text-container">
        <h1>{{title}}</h1>
        <h4>{{summary}}</h4>
        <ul data-type="tag-container">
            {{#each tag}}
            <li>{{this}}</li>
            {{/each}}
        </ul> 
    </div>
</section>